# 搭建步骤 —— 山大人文故事游戏后端骨架

一步步照做即可。目标：本地把项目跑起来，游客登录 + 统一认证（mock）+ 登录态鉴权全部打通。

---

## 第 0 步：装好环境

- **JDK 21**（项目用 21，低版本改 pom 里 `<java.version>`）
- **Maven 3.8+**
- **MySQL 8.0**
- IDE 建议 IntelliJ IDEA

验证：
```bash
java -version
mvn -version
mysql --version
```

---

## 第 1 步：建仓库

```bash
mkdir sdu-story-game && cd sdu-story-game
git init
```

然后按下面《目录结构》把文件一个个建出来（或直接用我打包的 zip 解压）。

---

## 第 2 步：建数据库

登录 MySQL，执行 `schema.sql`：
```bash
mysql -u root -p < schema.sql
```
它会建库 `sdu_story_game` 和 8 张表。验证：
```sql
USE sdu_story_game;
SHOW TABLES;   -- 应看到 user / chapter / story_node / story_choice /
               --        game_save / save_path / achievement / user_achievement
```

---

## 第 3 步：改配置

打开 `src/main/resources/application.yml`，改两处：
1. `spring.datasource.password` → 你本地 MySQL 密码
2. `app.jwt.secret` → 随便一串长随机字符（≥32 位）

`app.cas.shared-key` 和 `mock-enabled` 现在不用管——默认 `mock-enabled: true`，本地不依赖学长服务。

---

## 第 4 步：拉依赖 + 启动

```bash
mvn clean compile      # 第一次会下载依赖，用了阿里云镜像，较快
mvn spring-boot:run
```
看到 `Started StoryGameApplication` 就成功了，服务在 `http://localhost:8080`。

> 若依赖下载慢/失败：确认能访问 maven.aliyun.com，或在本机 `~/.m2/settings.xml` 配阿里云镜像。

---

## 第 5 步：测试三条链路

**① 游客登录**（无需任何前置）
```bash
curl -X POST http://localhost:8080/api/auth/guest
```
返回 `{"code":0,...,"data":{"token":"eyJ..."}}`，这个 token 就是你的登录态。

**② 用登录态访问受保护接口**
```bash
curl http://localhost:8080/api/user/me \
  -H "Authorization: Bearer 把上面的token粘这里"
```
返回当前用户信息 → 说明拦截器 + 鉴权打通。

不带 token 访问会返回 401，符合预期。

**③ 统一认证（mock）**
浏览器访问：
```
http://localhost:8080/api/auth/cas/callback
```
因为 mock 开着，会直接当成测试学号 `202400000000` 登录，然后重定向回前端地址并拼上 token。本地没前端页面的话，看浏览器地址栏最终 URL 上的 `token=` 即可。

---

## 第 6 步（以后）：接真统一认证

等要联调真认证时，才需要做这些（现在不用）：
1. 找学长（QQ 1017836943）申请：给他**回调接口公网地址**和一把 **key**，让他加进白名单。
2. `application-prod.yml` 里 `shared-key` 填这把 key，`mock-enabled: false`。
3. 本地想真跑：用 ngrok/frp 内网穿透给 `localhost:8080` 一个公网地址当回调。
4. 上线：换成服务器正式地址，`--spring.profiles.active=prod` 启动。

---

## 目录结构

```
sdu-story-game/
├── pom.xml                         Maven 配置（依赖 + 阿里云镜像）
├── schema.sql                      建库脚本（8 张表）
├── .gitignore
└── src/main/
    ├── java/edu/sdu/storygame/
    │   ├── StoryGameApplication.java     启动类
    │   ├── config/
    │   │   └── WebConfig.java            注册拦截器、放行登录接口
    │   ├── controller/
    │   │   ├── AuthController.java       游客登录 + CAS 回调（mock 开关）
    │   │   └── UserController.java       示例受保护接口 /api/user/me
    │   ├── service/
    │   │   └── AuthService.java          游客/CAS 用户查建 + 签发登录态
    │   ├── interceptor/
    │   │   └── AuthInterceptor.java      校验请求头 JWT
    │   ├── util/
    │   │   ├── CasJwtUtil.java           解学长的 10 秒 token（常量须与其一致）
    │   │   └── AppJwtUtil.java           我们自己的登录态 JWT 签/验
    │   ├── mapper/
    │   │   └── UserMapper.java           MyBatis-Plus 单表 CRUD
    │   ├── entity/
    │   │   └── User.java                 user 表实体
    │   └── dto/
    │       └── R.java                    统一返回包装
    └── resources/
        ├── application.yml               主配置
        ├── application-dev.yml           开发覆盖（mock 开）
        └── application-prod.yml          生产覆盖（mock 关，走环境变量）
```

## 两个 JWT 别搞混（重点）

| | CasJwtUtil | AppJwtUtil |
|---|---|---|
| 解/签谁的 token | 学长代理的 10 秒 token | 我们自己的登录态 |
| 密钥 | 和学长约定的 shared-key | app.jwt.secret（自己的） |
| 只解析还是签+验 | 只解析 | 签发 + 校验 |
| 常量能不能改 | salt/迭代次数/字段名**不能改** | 随便你 |

## 下一步能接着做的

- 剧情存档模块：chapter/node/choice 的实体 + mapper，存档读写接口，故事线三色查询
- 成就模块：achievement 实体 + 获得/进度接口
- 全局异常处理、跨域 CORS 配置（前端联调要用）
